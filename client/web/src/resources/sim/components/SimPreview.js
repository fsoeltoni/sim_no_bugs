import React from "react";
import { Typography } from "@material-ui/core";

const SimPreview = props => {
  return (
    <div style={{ width: 500, margin: "1em" }}>
      <Typography variant="h6">Tercetak</Typography>
      <Typography variant="body2">Preview SIM</Typography>
    </div>
  );
};

export default SimPreview;
